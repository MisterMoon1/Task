# Задача №3

def zeros(n):

  """Вычисляет факториал"""
  
  def fact(n):
    if n == 0:
      return 1
  return fact(n-1)*n    
  
  """Переворачивает строку и добавляет в счетчик совпадения""" 
  
  a = fact(n)
  c = str(a)[::-1]
  count = 0
  for i in c:
    if i == '0':
      count += 1
    else:
      break
    return(count)
